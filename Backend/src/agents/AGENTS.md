# Legacy Agent Directory

## Status
- This directory is legacy. Do not add new product logic here.
- New conversational orchestration belongs in `src/runtime/` and `src/services/`.

## Migration Rule
- If code here is still referenced, move the primary behavior to the canonical runtime and retire the duplicate path.
