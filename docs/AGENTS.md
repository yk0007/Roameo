# Docs

## Rules
- Documentation must describe the canonical SSE session runtime, shared contracts, and provider settings flow.
- Remove references to retired WebSocket, duplicate auth, or mock itinerary behavior when updating docs.
